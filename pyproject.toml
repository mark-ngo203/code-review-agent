[project]
name = "code-review-agent"
version = "0.1.0"
description = "Multi-agent code review system for GDG. Security and Performance Focused."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.3.0",
    "google-genai>=2.9.0",
    "pydantic>=2.13.4",
]
